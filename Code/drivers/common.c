/**
 *
 * @brief Common functions
 * @file common.c
 * @author hamster
 *
 * Functions that didn't fit anywhere else
 *
 */

#include "LPC13Uxx.h"
#include "lpc_types.h"
#include "drivers/common.h"

/**
 * Calculate a 16 bit CRC
 *
 * @param crc Rolling CRC value, seed with intial val
 * @param data_in Pointer to the data to checksum
 * @param data_len Number of bytes to CRC
 * @return The calculated CRC for the data input
 */
uint16_t crc_16_ccitt(uint16_t crc, uint8_t * data_in, uint16_t data_len){

  uint16_t i;

  for(i = 0; i < data_len; i++)
  {
    crc  = (unsigned char)(crc >> 8) | (crc << 8);
    crc ^= data_in[i];
    crc ^= (unsigned char)(crc & 0xff) >> 4;
    crc ^= (crc << 8) << 4;
    crc ^= ((crc & 0xff) << 4) << 1;
  }

  return crc;
}

/**
 * Use the msTicks ticker to busy-wait for the number of milliseconds
 *
 * @param ms Milliseconds to wait
 */
void delay_ms(uint32_t ms){
	TIME now;
	getTimeStamp(&now);

	while (!timeIsTimeout(&now, ms)){
		// Spin our wheels busy waiting
	}

	getTimeStamp(&now);
}

/**
 * Get the current system timestamp
 *
 * @param time Time structure to save the timestamp into
 */
void getTimeStamp(TIME *time){
	__disable_irq();
	time->Seconds = sysTime.Seconds;
	time->Milliseconds = sysTime.Milliseconds;
	__enable_irq();
}

/**
 * Get the difference in time from now to when a timestamp was set
 *
 * @param since The old timestamp
 * @param elapsed How much time has passed
 */
void timeElapsedSince(TIME *since, TIME *elapsed){
	TIME now;

	getTimeStamp(&now);

	// Did we roll over?
	if( ((now.Seconds == since->Seconds) && (now.Milliseconds < since->Milliseconds))
			|| (now.Seconds < since->Seconds)){
		// We've had a rollover, since now is less then then
		elapsed->Seconds = now.Seconds + (0xFFFFFFFF - since->Seconds);
		elapsed->Milliseconds = now.Milliseconds + (1000 - since->Milliseconds);
		if(elapsed->Milliseconds > 999){
			// Add a second
			elapsed->Milliseconds -= 1000;
			elapsed->Seconds++;
		}
	}
	else{
		if(now.Milliseconds < since->Milliseconds){
			elapsed->Milliseconds = now.Milliseconds + (1000 - since->Milliseconds);
			elapsed->Seconds = now.Seconds - since->Seconds - 1;
		}
		else{
			elapsed->Milliseconds = now.Milliseconds - since->Milliseconds;
			elapsed->Seconds = now.Seconds - since->Seconds;
		}
	}
}

/**
 * Determine if a timeout value has occurred since a timestamp
 *
 * @param time The timestamp of what to check against
 * @param timeout The timeout time, in milliseconds
 * @return True if the time has passed
 */
bool timeIsTimeout(TIME *time, uint32_t timeout){

	// Determine how much time has passed since the timestamp
	TIME difference;
	timeElapsedSince(time, &difference);

	return(((difference.Seconds * 1000) + difference.Milliseconds) >= timeout);
}

/**
 * Fast lookup table for reversing a single byte
 *
 * @param byte The byte to reverse
 * @return byte, bit-wise reversed
 */
unsigned char reverseByte(unsigned char byte){

    static const unsigned char table[] = {
        ,
    };
    return table[byte];
}




